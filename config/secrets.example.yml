# Speedrun Career Ops — Secrets Configuration
# Copy this file to config/secrets.yml and fill in your tokens.
# NEVER commit secrets.yml to git (it's gitignored).

# Typeform API token (required for /speedrun talent-network auto-submit)
# Get yours at: https://admin.typeform.com/account#/section/tokens
# Needs "Read responses" + "Create responses" scopes
typeform_api_token: ""

# Optional: Override the talent network form IDs
# (Only change these if a16z updates the forms)
# typeform_form1_id: "uPI8kFOI"
# typeform_form2_id: "b20t87QG"
